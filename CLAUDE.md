## Project

Type: vite
Deploy: github-pages

## Structure

src/app.js, src/components/[Name]/[Name].js+css, src/styles/, src/utils/, src/data/, public/images/, public/videos/

## Rules

- Uses Locomotive Scroll for smooth scrolling, never use native scrollIntoView
- CSS vars from settings widget, never hardcode colors
- MUST have favicon.svg + favicon.png + favicon.ico in public/images/
